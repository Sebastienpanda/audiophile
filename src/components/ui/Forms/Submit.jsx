export function Submit({ content }) {
    return (
        <button className="btn bg-primary text-shop uppercase text-white transition-colors duration-[350ms] ease-in-out hover:bg-hoverBtn">
            {content}
        </button>
    )
}
